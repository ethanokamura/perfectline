---
import Tag from './Tag.astro'
const { data, url } = Astro.props;
---

<a href={`${url}`} class='bg-secondary rounded w-72 font-manrope mx-auto items-start flex flex-col gap-4 drop-shadow-none hover:scale-110 ease-linear duration-100'>
  <img class="rounded-t-lg" src={data.img}/>
  <div class="text-start px-6 pb-4 flex flex-col justify-between">
    <div class="grid gap-4">
      <h1 class="capitalize text-xl drop-shadow-none text-text font-rubik font-[800]">{data.title}</h1>
      <p class="text-sm capitalize text-text2">{data.description}</p>
    </div>
    <div class="tags overflow-y-scroll">
      {data.tags.map((tag: string) =>
        <Tag name={tag}/>
      )}
    </div>
  </div>
</a>
