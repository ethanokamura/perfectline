---
const { name } = Astro.props;
let css;
let tagID = name;
if (tagID == "cpp") tagID = "c++";
if (tagID == "cs") tagID = "c#";
---

<span class={`tag text-xs bg-accent dark:bg-darkaccent ${css}`}>{tagID}</span>
