---
import { getEntry, render } from 'astro:content';
import InfoLayout from '../layouts/InfoLayout.astro';
const entry = await getEntry('info', 'tutoring');
const { Content } = await render(entry);
---
<InfoLayout frontmatter={entry}>
  <Content />
</InfoLayout>
